<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.richard.project" >

    <application
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name">
        <activity
            android:name=".CarbonCalcMainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".CarbonCalcAddRowActivity" />
        <activity android:name=".ContactsMainActivity" />
        <activity android:name=".ContactsAddRowActivity" />
        <activity android:name=".TipCalcMainActivity" />
        <activity android:name=".TipCalcAddRowActivity" />
    </application>

</manifest>

